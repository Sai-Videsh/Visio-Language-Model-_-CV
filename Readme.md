IMprovements that can be made

more epochs , more data total, low learning rate, threshold reduce (0.5 to 0.35), incr in img size, 

Data augmentation - random flips, jitter, etc.. kind of things (take avg of orig image pred, and this one)

class imbalance - solved using BCE loss, tversky, etc.. kind of things

use pre trained model  instead of UNET

